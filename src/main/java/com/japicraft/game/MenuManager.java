package com.japicraft.game;

import net.minestom.server.entity.Player;

public class MenuManager {
    public static void openMain(Player player) {
        player.sendMessage("Use mouse to move the cursor.");
    }
}
